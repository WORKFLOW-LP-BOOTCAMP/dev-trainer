<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="#">Trainer dev</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarCollapse">
			<ul class="navbar-nav">
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('trainer_trainers') }}">Trainers</a>
				</li>
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('app_article') }}">articles</a>
				</li>
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('app_admin_trainers') }}">Admin</a>
				</li>
				{% if app.user %}
					<li class="nav-item active">
						<a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
					</li>
				{% else %}
					<li class="nav-item active">
						<a class="nav-link" href="{{ path('app_login') }}">Login</a>
					</li>
				{% endif %}
				{% if app.user %}
					<li class="nav-item active text-color-white">
						<a class="nav-link">Connect :
							{{ app.user.firstName }}
						</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
