{% extends 'base.html.twig' %}

{% block title %}Hello TrainerController!{% endblock %}

{% block body %}

{# 
   {% for trainer in trainers %}
    <p>{{ trainer.firstName }}</p>
    {% if trainer.articles %}
        <ul>
        {% for article in trainer.articles %}
            <li>{{ article.title }}</li>
        {% endfor %}
        </ul>
    {% endif %}
   {% endfor %}

#}

<div class="container">
		<div class="row">
			<div class="col-md">
				{% for trainer in trainers %}
					<div class="card">
						<h5 class="card-header">{{ trainer.profession }}</h5>
						<div class="card-body">
							<h5 class="card-title">{{ trainer.firstName }}
								{{ trainer.lastName }}</h5>
							<p class="card-text">{{ trainer.bio }}</p>
						</div>
					</div>
				{% endfor %}
			</div>
			<div class="col-md">
				
			</div>
		</div>
	</div>
{% endblock %}
